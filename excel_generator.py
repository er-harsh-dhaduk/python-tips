# import xlsxwriter## student_xlx = xlsxwriter.Workbook("student.xlsx")## student_sheet = student_xlx.add_worksheet()## student_header = ["Student Name", "Marks"]# student_list = [["Student1", "40"],["Student2", "50"],["Student3", "60"],#                 ["Student4", "70"]]## row = 0# column = 0## for header in student_header:#     student_sheet.write(row, column, header)#     column += 1## row = 1# for student in student_list:#     student_sheet.write(row, 0, student[0])#     student_sheet.write(row, 1, student[1])#     row += 1## student_xlx.close()import xlrdworkbook = xlrd.open_workbook('student.xlsx')worksheet = workbook.sheet_by_name('Sheet1')worksheet = workbook.sheet_by_index(0)for rx in range(worksheet.nrows):   print(worksheet.row(rx)[0].value)